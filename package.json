{
  "name": "places-management",
  "version": "1.0.0",
  "description": "Система управления местами с защищенной админ-панелью",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch config --watch middleware --watch routes --watch utils --ignore data --ignore images --ignore public server.js",
    "dev:verbose": "nodemon --verbose --watch server.js --watch config --watch middleware --watch routes --watch utils --ignore data --ignore images --ignore public server.js"
  },
  "keywords": [
    "places",
    "management", 
    "admin",
    "express",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "bcryptjs": "^2.4.3",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "express-session": "^1.17.3",
    "express-validator": "^7.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}